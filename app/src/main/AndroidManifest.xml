<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 必要权限：定位、WiFi、语音、网络 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 🆕 添加 Android 6.0+ 必需的权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- 🆕 针对旧设备的兼容性声明 -->
    <uses-feature android:name="android.hardware.wifi" android:required="true" />

    <!-- 适配盲人用户：启用触摸浏览 -->
    <uses-feature android:name="android.software.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.audio.output" android:required="true" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IndoorNavBlind"
        android:accessibilityFeedbackType="feedbackSpoken"
        tools:targetApi="31">

        <!-- 主界面 -->
        <activity
            android:name="com.example.indoornavblind.ui.activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.IndoorNavBlind.NoActionBar"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 设置界面（备用，当前集成在MainActivity） -->
        <activity
            android:name="com.example.indoornavblind.ui.activities.R_SettingsActivity"
            android:exported="false" />
    </application>
</manifest>